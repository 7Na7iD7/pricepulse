services:
  pricepulse-api:
    build: .
    container_name: pricepulse-api
    ports:
      - "8000:8000"
    volumes:
      - ./artifacts:/app/artifacts:ro
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
